# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Job Application Tracker' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Job Application Tracker

pod 'Firebase/Auth'
pod 'Firebase/Database'

  target 'Job Application TrackerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Job Application TrackerUITests' do
    # Pods for testing
  end

end
